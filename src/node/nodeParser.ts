import { NodeConfig } from "../common/config.js";
import { Parser } from "../common/parser.js";

export default class NodeParser extends Parser<NodeConfig> {
    constructor() {
        super()
    }
}